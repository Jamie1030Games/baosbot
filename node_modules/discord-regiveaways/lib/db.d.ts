import { Giveaway } from './types/Giveaway';
declare class db {
    path: string;
    InitFilePath(path: string): void;
    private getFilePath;
    private readGiveawayFile;
    private writeGiveawayFile;
    AddEntries(giveawayId: string, user: string): void;
    RemoveEntries(giveawayId: string, userId: string): string[];
    GetGiveawayData(giveawayId: string): Giveaway;
    Create(giveaway: Giveaway, giveawayId: string): void;
    SetEnded(giveawayId: string, state: boolean | string): string;
    SetWinners(giveawayId: string, winners: string[] | string): string;
    GetAllGiveawaysData(): {
        giveawayId: string;
        giveawayData: Giveaway;
    }[];
    DeleteGiveaway(giveawayId: string): void;
    AvoidDoubleEntries(giveawayId: string): void;
}
declare const _default: db;
export default _default;
